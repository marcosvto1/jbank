spring:
  application:
    name: jbank

  datasource:
    url: jdbc:mysql://localhost:3306/jbankdb
    username: jbank
    password: secret
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true